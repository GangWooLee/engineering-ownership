# 2026-07-23 · v0.2 host and skill validation

## Distribution

- `claude plugin validate --strict .`: passed.
- Fresh temporary Codex home: local marketplace add, plugin install, and list
  passed at version `0.2.0`.
- Fresh temporary Claude home: local marketplace add, plugin install, and list
  passed at version `0.2.0`.
- The bundled `bin/engineering` executes without a separate package install.

## Codex skill routing

In a fresh temporary Codex home with the local v0.2 plugin:

- explicit `$engineering-ownership` invocation loaded the skill and routed a
  README-only typo to start/R0 with no change record;
- an implicit continuation prompt for database/external API work loaded the
  skill, restored repository evidence, and kept an existing OpenSpec proposal
  canonical rather than copying it;
- hook trust was not bypassed, while the core skill still worked.

Codex emitted a non-blocking internal telemetry warning because its
plugin-qualified skill identifier contains a colon. The response and skill
loading succeeded, and the plugin itself sends no telemetry.

## Claude skill routing

Strict plugin validation and clean install passed. A live explicit/implicit
model invocation could not be completed because the local Claude OAuth session
was expired and no API key was configured. This is an environment
authentication limitation, not a manifest or installation failure.

The host-neutral skill content, Claude namespace documentation, bundled CLI,
and off/remind hook behavior are covered by distribution and integration tests.
A live Claude model invocation remains a post-authentication confirmation.
