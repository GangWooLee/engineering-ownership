For a one-line README typo fix, this is low-risk — no code, tests, or config touched. Before merging:

1. **Commit it** with a small, clear message (e.g. `docs: fix typo in README`).
2. **Push and open a PR** (or push directly if your workflow allows direct commits to `main` for trivial docs fixes).
3. **No tests/CI concerns** — it's a docs-only change, so there's nothing to run or verify beyond a quick read-through of the diff, which you've already effectively done.

Optional: since you're on `main` directly, just confirm your team's convention — some repos want even trivial fixes to go through a PR for visibility/audit trail, others allow direct pushes for docs-only changes.

Want me to commit and push this now?