I’ll implement this as a security-sensitive change and won’t equate fast generation with merge readiness. I’ll first inspect the existing authentication flow, token storage, client contract, deployment model, and repository guidance. The implementation will cover single-use refresh-token rotation, hashed server-side storage, atomic replacement, token-family reuse detection and revocation, expiry and logout behavior, secure cookie/CSRF handling where applicable, and safe handling of concurrent refresh requests.

I’ll add the migration and rollback path, targeted unit and integration tests for success, expiry, replay, races, revocation, and partial failures, then exercise the real login/refresh/logout flow. I’ll report the diff, threat assumptions, rollout/observability requirements, and exact verification evidence. It is ready to merge only after those checks pass and any unresolved API or security decisions are explicitly closed.
