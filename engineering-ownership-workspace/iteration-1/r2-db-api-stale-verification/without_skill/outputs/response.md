I can’t honestly mark it complete from earlier test results. Database-schema and partner-API changes can drift after the last run, and existing test files are not evidence that the current code passed them.

I’ll inspect the current diff and repository instructions, verify the migration forward and rollback behavior against representative data, check compatibility across the deployment window, validate the adapter against the partner contract and failure modes, and run the relevant tests on the current working tree. I’ll also review generated schema artifacts and configuration for unintended changes. I’ll report completion only with fresh command results and any remaining limitations; if required services or credentials are unavailable, I’ll leave the work explicitly unverified rather than calling it done.
