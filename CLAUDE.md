# Repository instructions

<!-- engineering-ownership:start -->
Use the installed `engineering-ownership` skill and
`.engineering/contract.json` for non-trivial changes. The CLI is Python 3.11+
standard-library-only. Preserve no-shell execution, repository-contained
writes, no telemetry, matching Codex/Claude/package versions, and reminder
hooks that remain no-op unless the project explicitly sets
`automation.session_hooks` to `remind`.
<!-- engineering-ownership:end -->
